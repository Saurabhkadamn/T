# Kadal AI — Deep Research Microservice
# Python 3.11+
# Pinned for reproducible production builds. Update via: pip install -r requirements.in && pip freeze

# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ── Pydantic / settings ────────────────────────────────────────────────────
pydantic==2.10.6
pydantic-settings==2.8.1

# ── LangGraph / LangChain ──────────────────────────────────────────────────
langgraph==1.0.10
langgraph-checkpoint-mongodb==0.3.1
langchain-mongodb==0.3.2          # explicit: transitive dep of langgraph-checkpoint-mongodb
langchain==0.3.21
langchain-core==0.3.47
langchain-google-genai==1.0.10    # intentionally 1.x — avoids google-genai SDK 4.x rewrite
langchain-openai==0.2.14          # intentionally 0.2.x — stays on openai v1 SDK
langchain-anthropic==0.2.4        # intentionally 0.2.x
langchain-community==0.3.21       # matches langchain==0.3.21

# ── MongoDB ────────────────────────────────────────────────────────────────
motor==3.7.1
pymongo==4.15.0                   # pinned: motor needs <5.0, checkpoint-mongodb needs <4.16

# ── Redis ──────────────────────────────────────────────────────────────────
redis==5.2.1

# ── HTTP ───────────────────────────────────────────────────────────────────
httpx==0.28.1

# ── Search tools ───────────────────────────────────────────────────────────
arxiv==2.2.0
pymupdf==1.25.3

# ── AWS ────────────────────────────────────────────────────────────────────
boto3==1.37.26

# ── Environment ────────────────────────────────────────────────────────────
python-dotenv==1.1.0

# ── Observability ────────────────────────────────────────────────────────────
langfuse==3.14.5
grpcio==1.71.0
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-exporter-otlp-proto-grpc==1.40.0
opentelemetry-instrumentation-fastapi==0.61b0
opentelemetry-instrumentation-httpx==0.61b0
opentelemetry-instrumentation-pymongo==0.61b0
opentelemetry-instrumentation-redis==0.61b0
opentelemetry-instrumentation-logging==0.61b0
